# Automatically added by dh_installinit
if [ -x "/etc/init.d/logstash-forwarder" ]; then
	update-rc.d logstash-forwarder defaults >/dev/null
	invoke-rc.d logstash-forwarder start || exit $?
fi
# End automatically added section
